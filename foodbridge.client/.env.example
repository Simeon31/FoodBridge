# Environment configuration for FoodBridge client
# Copy to .env and adjust as needed

# Backend API base URL (include /api suffix)
VITE_API_URL=https://localhost:7066/api

# Alternate if using HTTP only
# VITE_API_URL=http://localhost:5231/api

# When deploying behind reverse proxy serving both frontend & backend
# you can omit and rely on relative path
# VITE_API_URL=/api
